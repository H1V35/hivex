name: Quality

on:
  workflow_dispatch:
  pull_request:
    types: [opened, synchronize, reopened, ready_for_review]
  push:
    branches: [main]

permissions: {}

concurrency:
  group: quality-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

jobs:
  quality:
    runs-on: [self-hosted, macOS, ARM64, hivex]
    env:
      DEVELOPER_DIR: /Library/Developer/CommandLineTools
    timeout-minutes: 15
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@93cb6efe18208431cddfb8368fd83d5badbf9bfd # v5
        with:
          fetch-depth: 0
          persist-credentials: false
          ref: ${{ github.event.pull_request.head.sha || github.sha }}
      - uses: oven-sh/setup-bun@0c5077e51419868618aeaa5fe8019c62421857d6 # v2
        with:
          bun-version: 1.4.2
          no-cache: true
      - name: Install dependencies
        run: bun ci
      - name: Typecheck
        run: bun run --bun typecheck
      - name: Lint
        run: bun run --bun lint
      - name: Format
        run: bun run --bun format:check
      - name: Rust toolchain
        run: |
          echo "$HOME/.cargo/bin" >> "$GITHUB_PATH"
          echo "/opt/homebrew/bin" >> "$GITHUB_PATH"
          "$HOME/.cargo/bin/rustup" toolchain install stable --profile minimal --component clippy --component rustfmt
      - name: Rust format
        run: cargo fmt --check
      - name: Rust lint
        run: cargo clippy --locked --all-targets -- -D warnings
      - name: Rust test
        run: cargo test --locked
      - name: Native package
        run: bun run pack:native
      - name: CLI contracts on the packaged executable
        run: HIVEX_TEST_BINARY="$PWD/target/aarch64-apple-darwin/release/hivex" bun test --timeout 15000 ./test
      - name: Installed native package
        run: bun run verify:package
